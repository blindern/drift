# TYPE  DATABASE        USER            ADDRESS         METHOD
local      all           all                             trust
# pgadmin: auth handled by SAML on foreningenbs.no/tools
host       all           all             172.25.16.57/32 trust
# fbs-p print server: libpq 9.3 cannot do SCRAM (auth method 10). Uses its own
# md5-only roles so pykotaadmin can stay SCRAM for intern.
host       pykota        pykotauser,pykotaadminlegacy  172.25.10.2/32  md5
host       all           all             all             scram-sha-256
